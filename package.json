{
  "name": "skadequest",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0"
  }
}
